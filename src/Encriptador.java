public class Encriptador {
    public StringBuilder encripter(String text,int mecanismo){
        StringBuilder nuevotxt = new StringBuilder();
        for(int i=0;i<text.length();i++){
            char temp =(char) (text.charAt(i)+2);
            if(){
                
            }
            nuevotxt.append(temp);
        }
    }
}
